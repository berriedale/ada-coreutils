
project Pwd is
   for Source_Dirs use ("../src");
   for Object_Dir use "../build/obj";

   for Main use ("pwd.adb");

   package Linker is
      for Default_Switches ("ada") use ("-fprofile-generate");
   end Linker;

end Pwd;
